name: secrets
description: |
  Hardcoded credential / token detection — cloud provider keys, OAuth
  tokens, PEM private keys, JWT, generic password/api_key assignments,
  and DB connection strings with embedded passwords.
filters:
  paths_under:
    - rules/secrets/
exclude:
  rule_id_globs: []
