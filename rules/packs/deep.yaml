name: deep
description: |
  Everything. Used by /audit-deep for full pre-release audits and pentest
  engagements. Expect high finding volume — pair with the triage subagent.
filters: {}
exclude:
  rule_id_globs: []
